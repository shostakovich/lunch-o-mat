suites:
    main:
        namespace: App
        psr4_prefix: App
        src_path: app
        spec_path: app
        spec_prefix: spec

extensions:
    - PhpSpec\Laravel\Extension\LaravelExtension

laravel_extension:
    testing_environment: 'testing'

formatter.name: pretty